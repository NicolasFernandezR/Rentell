services:
  postgresDB:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=db_rentell
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - "5436:5432"